# API reference entrance

For the complete API reference, see: [api-reference.md](./api-reference.md).

This file is used for compatibility with `./api.md` links in historical documents.

---

## Corresponding sample file

**Example entry**: [api.ts](https://github.com/devcodex-labs/schema-dsl/blob/main/examples/docs/api.ts)
**Instructions**: Connect the most commonly used public APIs `s()`, `validate()`, `Validator.validateAsync()` and `TypeConverter` with the smallest entry.
